function solve(num1, num2, num3) {
    let dumata = '';
    if (num1 * num2 * num3 >= 0) {
        dumata = 'Positive';
    } else {
        dumata = 'Negative'
    }
    console.log(dumata)
}
solve(-1,
    0,
   1
    )