function solve(input) {
    let lostFights = parseInt(input.shift());
    let helmetPrice = parseFloat(input.shift());
    let swordPrice = parseFloat(input.shift());
    let shieldPrice = parseFloat(input.shift());
    let armorPrice = parseFloat(input.shift());

    newHelmetPrice = helmetPrice;
    newSwordPrice = swordPrice;
    newShieldPrice = shieldPrice;
    newArmorPrice = armorPrice;

    for (let i = 0; i < lostFights.length; i++) {
        for (let helmetBroke = 0; helmetBroke < array.length; helmetBroke++) {
            const element = array[helmetBroke];
            
        }
    }
}