function solve(arr) {
    // for (let firstString = 0; firstString < arr.length / 2; firstString++) {
    // //    swap
    // }
    arr.reverse()
    console.log(arr.join(' '))
}
solve(['abc', 'def', 'hig', 'klm', 'nop'])

